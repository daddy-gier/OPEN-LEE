# OPEN-LEE Desktop App — Assets

For a professional desktop app, you should add an icon. 

## To Add Custom Logo

1. Create a 256x256 PNG image named `logo.png`
2. Place it in the `assets/` folder
3. On Windows, Electron Forge auto-converts it to .ico

For now, the app will run without a custom icon (uses default).
